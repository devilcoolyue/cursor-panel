[project]
name = "cursor-panel-desktop-sidecar"
version = "0.0.9"
license = "MIT"
requires-python = ">=3.12,<3.13"
dependencies = ["cursor-dashboard", "keyring>=25,<26", "psutil>=7,<8", "pyinstaller>=6.16,<7"]

[dependency-groups]
dev = ["psutil>=7,<8"]

[tool.uv]
package = false

[tool.uv.sources]
cursor-dashboard = { path = "../..", editable = true }
